#include <bits/stdc++.h>
using namespace std;

int getBuildingArrangements(int n) {

  //* this is the same as count binary strings, exact same
}

int main() {

  cout << getBuildingArrangements(3);
  return 0;
}
