#include <bits/stdc++.h>
using namespace std;

//* here the set can contain duplicate nummbers also, so may end up with same combinations, you have to prevent it, i guess this will be same as subset 2 problem

int main()
{
    return 0;
}